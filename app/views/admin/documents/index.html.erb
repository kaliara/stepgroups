<h1>Listing documents</h1>

<table>
  <tr>
    <th>Intergroup session</th>
    <th>Title</th>
    <th>Document type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @documents.each do |document| %>
  <tr>
    <td><%= document.intergroup_session.name %></td>
    <td><%= document.title %></td>
    <td><%= document.document_type %></td>
    <td><%= link_to 'Show', document_path(document_path) %></td>
    <td><%= link_to 'Edit', edit_document_path(document) %></td>
    <td><%= link_to 'Destroy', document_path(document_path), confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Document', new_document_path %>
