<h1>Listing sections</h1>

<table>
  <tr>
    <th>Document</th>
    <th>Heading</th>
    <th>Body</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sections.each do |section| %>
  <tr>
    <td><%= link_to section.document.title, admin_document_path(section.document) %></td>
    <td><%= section.heading %></td>
    <td><%= section.body %></td>
    <td><%= link_to 'Show', admin_section_path(section) %></td>
    <td><%= link_to 'Edit', edit_admin_section_path(section) %></td>
    <!-- <td><%= link_to 'Remove', admin_section_path(section), :confirm => 'Are you sure?', :method => :delete %></td> -->
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Section', new_admin_section_path %>
