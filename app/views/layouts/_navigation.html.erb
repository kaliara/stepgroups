<div id="nav">
  <a id="logo" href="/pages/what_is_coda"><%= image_tag "logo.png", :alt => "NYCODA" %></a>
  <% unless Menu.get_items(0).empty? %>
    <ul>
      <% Menu.get_items(0).each do |item| %>
        <li><%= link_to item.label, item.path %></li>
      <% end %>
    </ul>
  <% end %>
  <% unless Menu.get_items(1).empty? %>
    <ul>
      <% Menu.get_items(1).each do |item| %>
        <li><%= link_to item.label, item.path %></li>
      <% end %>
    </ul>
  <% end %>
</div>